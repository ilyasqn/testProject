apiVersion: v1
kind: ConfigMap
metadata:
  name: user-service-config
  namespace: pet
data:
  DB__URL: "postgresql+asyncpg://postgres:admin@user-db:5432/user_db"
  JWT__SECRET_KEY: "382ca91f3321b96c83b412e8c058c8006521e6156b995c1d25d659481551d708"
  JWT__ALGORITHM: "HS256"
  JWT__ACCESS_TOKEN_EXPIRE_MINUTES: "30"
  JWT__REFRESH_TOKEN_EXPIRE_HOURS: "24"
  RABBITMQ__URL: "amqp://guest:guest@rabbitmq:5672/"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: pet
spec:
  replicas: 4
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: user-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8001
          envFrom:
            - configMapRef:
                name: user-service-config
          resources:
            requests:
              memory: "128Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /docs
              port: 8001
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: pet
spec:
  selector:
    app: user-service
  ports:
    - port: 8001
      targetPort: 8001
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: user-service-hpa
  namespace: pet
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: user-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
